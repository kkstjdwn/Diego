<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="productMapper">
  
  <!--insert  -->
  <insert id="productInsert" parameterType="ProductVO">
  		insert into product(pro_num, cat_ref, pro_name, pro_price, pro_count,  pro_date, pro_contents, pro_warning, opt1, opt1_val, opt2, opt2_val, opt3, opt3_val) 
  		values(product_seq.nextval, #{cat_ref},#{pro_name}, #{pro_price}, #{pro_count},sysdate, #{pro_contents}, #{pro_warning}, #{opt1}, #{opt1_val}, #{opt2}, #{opt2_val}, #{opt3}, #{opt3_val} )
 </insert>
  
  <!--delete  -->
  <delete id="productDelete" parameterType="Integer">
  		delete product where pro_num=#{pro_num}
  
  </delete>
  
  <!--list  -->
  <select id="productList" parameterType="" resultType="">
  	
  </select>
  
  
  </mapper>
  
  

  
  
 