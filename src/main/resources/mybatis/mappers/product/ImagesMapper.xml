<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

  <mapper namespace="imagesMapper">
  
  
 
  	
  	<!--이미지 insert  -->
	<insert id="imagesInsert">
		
		insert into images(pro_num, pro_main, sumnale, front, back, pro_model, pro_full, gif, pro_info, pro_size)
		values(#{pro_num},#{pro_main},#{sumnale},#{front},#{back},#{pro_model},#{pro_full},#{gif},#{pro_info},#{pro_size} )
	
	</insert>  	
  	
  	
  
  
  </mapper>